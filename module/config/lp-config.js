// path: ./module/config/lp-config.js
// ./module/Config/lp-config.js

// Define the protocol
export const protocol = "http://"; // Change this to "https://" if needed

export const appName = "litPress";
export const appURL = `${protocol}localhost:5173`; // or "litpress.com".
export const appAPI = `${protocol}localhost:3000`; // or "api.litpress.com".

export const database = "firebase"; // or 'sqlite', 'mongodb', 'mysql', etc.;

export const singleContentBG = "#fff"; // or 'red', #5fff34, etc.;
